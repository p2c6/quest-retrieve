export type UserLogin  = {
    email: string;
    password: string;
}

export type UserRegistration = {
    email: string;
    password: string;
    last_name: string;
    first_name: string;
    birthday: string;
    contact_no: number;
}